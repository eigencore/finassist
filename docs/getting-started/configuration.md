# Configuration

This document provides an overview of the configuration options available for the project. The configuration is managed through a `.env` file, which allows you to set environment variables that control various aspects of the application.

## Big Query Configuration